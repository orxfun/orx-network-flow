# MCNF Demo

Small interactive demo for `orx-network-flow`.

## Run

```bash
cd examples/mcnf-demo
npm install
npm run dev
```

This starts the app with `trunk serve`.

## Notes

- The demo is under active development and the UI/workflow may change.
- Solver support depends on enabled Cargo features; the default setup uses `microlp`.
