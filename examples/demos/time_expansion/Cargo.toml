[package]
name = "time_expansion"
version = "0.1.0"
edition = "2024"
publish = ["artifactory"]

[dependencies]
derive-new = { version = "0.7.0", registry = "artifactory" }
leptos = { version = "0.6", features = ["csr"] }
orx-network-flow = { path = "../../.." }
